{
  "name": "iitm-invest-arena",
  "private": true,
  "version": "1.0.0",
  "description": "IITM Invest Arena — investment simulation competition platform",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix server start",
    "seed": "npm --prefix server run seed",
    "reset": "npm --prefix server run reset"
  }
}
